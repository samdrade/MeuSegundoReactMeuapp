//funcion addCurtida(id) {
   // const novaListaPersonagens = personagens.map(personagem => {
     //   return personagem.id === id ? {...personagem, favorite:!personagem.favorite}:personagem
    //})
   // setPersonagem(novaListaPersonagens)
//}
